# ODS adds Linux netlink only so fixed read-only host inventory actions can
# inspect interface and route state. The unprivileged broker retains an empty
# capability set, including no CAP_NET_ADMIN authority.
[Service]
RestrictAddressFamilies=
RestrictAddressFamilies=AF_UNIX AF_INET AF_INET6 AF_NETLINK
